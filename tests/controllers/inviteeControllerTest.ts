import { getInvitees } from '../../src/controllers/inviteeController';
import { expect } from 'chai';
import Request from 'supertest';


describe("Get Invitees", () => {
    it("Should return list of invitees", () => {
        expect(true).to.be.true;
    })
})